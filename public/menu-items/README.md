# Local menu item images

Place optional local menu item images in this directory and save their database path as `/menu-items/<filename>`.

Use web-friendly image formats and filenames. The application displays its built-in food placeholder when no image path is configured.
