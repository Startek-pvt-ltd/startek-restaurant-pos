# API

No application API endpoints are included in the foundation release. Future endpoints should live in App Router route handlers, validate inputs with Zod, keep business logic in feature services, and return consistent typed error responses.
