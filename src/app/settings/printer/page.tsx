import { PrinterForm } from "@/components/settings/SettingsForms";import { SettingsHeader } from "@/components/settings/SettingsNav";import { getSettingsBundle } from "@/features/settings/services/settings-service";import { OPERATIONAL_SETTINGS_ROLES } from "@/features/settings/types";import { requireRole } from "@/lib/auth-utils";
export default async function Page(){await requireRole(OPERATIONAL_SETTINGS_ROLES);const s=await getSettingsBundle();return <div className="space-y-5"><SettingsHeader title="Printer setup" description="Configure browser printing or the loopback ESC/POS bridge for receipt cutting and cash-drawer control."/><PrinterForm canEdit initial={s.printer}/></div>}
