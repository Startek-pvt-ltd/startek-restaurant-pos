import { FileSearch } from "lucide-react";

export function ReportTable({ caption, headers, rows }: { caption: string; headers: string[]; rows: React.ReactNode[][] }) {
  if (!rows.length) return <section className="rounded-2xl border border-dashed border-input bg-card px-5 py-14 text-center"><span className="mx-auto flex size-14 items-center justify-center rounded-2xl bg-muted text-muted-foreground"><FileSearch aria-hidden="true" className="size-6" /></span><h2 className="mt-4 text-lg font-black text-secondary">No report data</h2><p className="mt-1 text-sm text-muted-foreground">Try a different date range or clear the current filters.</p></section>;
  return <section className="report-table overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><div className="overflow-x-auto dashboard-scrollbar"><table className="w-full min-w-[900px] border-collapse text-left"><caption className="sr-only">{caption}</caption><thead className="bg-secondary text-[0.68rem] uppercase tracking-wider text-white/75"><tr>{headers.map((header) => <th className="whitespace-nowrap px-4 py-3.5 font-black" key={header} scope="col">{header}</th>)}</tr></thead><tbody className="divide-y divide-border">{rows.map((row, rowIndex) => <tr className="transition hover:bg-muted/35" key={rowIndex}>{row.map((cell, cellIndex) => <td className="whitespace-nowrap px-4 py-3.5 text-sm font-semibold text-secondary" key={cellIndex}>{cell}</td>)}</tr>)}</tbody></table></div></section>;
}
