import Link from "next/link";import { Building2,CreditCard,DatabaseBackup,Download,MonitorCog,Printer,ReceiptText,UserRound } from "lucide-react";import type { UserRole } from "@/generated/prisma/client";
export const settingsLinks=[["Restaurant","/settings/restaurant",Building2],["Receipt","/settings/receipt",ReceiptText],["Billing","/settings/billing",CreditCard],["Printer","/settings/printer",Printer],["System","/settings/system",MonitorCog],["Backup","/settings/backup",DatabaseBackup],["Data export","/settings/data-export",Download],["Profile","/settings/profile",UserRound]] as const;
export function SettingsNav({role}:{role:UserRole}){const links=role==="CASHIER"?settingsLinks.filter(([,href])=>href==="/settings/profile"):settingsLinks;return <nav aria-label="Settings sections" className="no-print flex gap-2 overflow-x-auto rounded-2xl border border-border bg-white p-2 shadow-sm">{links.map(([label,href,Icon])=><Link className="flex shrink-0 items-center gap-2 rounded-xl px-3 py-2 text-sm font-bold text-secondary transition hover:bg-primary/15 focus-visible:outline-2 focus-visible:outline-primary" href={href} key={href}><Icon className="size-4"/>{label}</Link>)}</nav>}
export function SettingsHeader({title,description}:{title:string;description:string}){return <header><p className="text-xs font-black uppercase tracking-[.18em] text-primary">System settings</p><h1 className="mt-2 text-2xl font-black text-secondary sm:text-3xl">{title}</h1><p className="mt-1 text-sm text-muted-foreground">{description}</p></header>}
