import type { OrderDetailRecord } from "@/features/orders/types";import { ReceiptRow } from "./ReceiptRow";
export function ReceiptTotals({money,order}:{money:(v:string|null|undefined)=>string;order:OrderDetailRecord}){const d=Number(order.discount)>0,t=Number(order.tax)>0,s=Number(order.serviceCharge)>0;return <section className="receipt-section receipt-summary"><ReceiptRow label="Subtotal" value={money(order.subtotal)}/>{d&&<ReceiptRow label="Discount" value={`-${money(order.discount)}`}/>} {t&&<ReceiptRow label="Tax" value={money(order.tax)}/>} {s&&<ReceiptRow label="Service Charge" value={money(order.serviceCharge)}/>}<ReceiptRow label="GRAND TOTAL" strong value={money(order.grandTotal)}/></section>}
