import Image from "next/image";
export function ReceiptHeader({printLogo,priority,restaurant,headerMessage}:{printLogo:boolean;priority:boolean;restaurant:{name:string;addressLine1:string;addressLine2:string|null;city:string;phone:string;phone2:string|null;logo:string|null};headerMessage:string}){const address=[restaurant.addressLine1,restaurant.addressLine2,restaurant.city].filter(Boolean).join(", ");const phones=[restaurant.phone,restaurant.phone2||"0778375427"].filter(Boolean).join(" / ");return <header className="receipt-section receipt-header">{printLogo&&<Image alt={restaurant.name} className="receipt-logo" height={76} priority={priority} src={restaurant.logo||"/logos/rice-kottu-hut-logo.png"} width={76}/>}<h1>{restaurant.name}</h1><p>{address}</p><p>{phones}</p>{headerMessage&&<p className="receipt-message">{headerMessage}</p>}</header>}
