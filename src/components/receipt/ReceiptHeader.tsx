import Image from "next/image";
export function ReceiptHeader({printLogo,priority,restaurant,showPhone,headerMessage}:{printLogo:boolean;priority:boolean;restaurant:{name:string;address:string;phone:string;logo:string|null};showPhone:boolean;headerMessage:string}){return <header className="receipt-section receipt-header">{printLogo&&<Image alt={restaurant.name} className="receipt-logo" height={76} priority={priority} src={restaurant.logo||"/logos/rice-kottu-hut-logo.png"} width={76}/>}<h1>{restaurant.name}</h1><p className="whitespace-pre-line">{restaurant.address}</p>{showPhone&&<p>{restaurant.phone}</p>}{headerMessage&&<p className="receipt-message">{headerMessage}</p>}</header>}
